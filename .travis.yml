# Created with https://github.com/dart-lang/mono_repo
language: dart
dart:
  - stable
  - 2.5.0
  - dev
sudo: required
addons:
  chrome: stable

script:
  - ./tool/travis.sh

cache:
  directories:
    - $HOME/.pub-cache